import ComingSoon from "@/components/ui/ComingSoon";
import { PAGE_METADATA } from "@/config/metadata";

export const metadata = PAGE_METADATA.help-center;
export default function Page() {
  return <ComingSoon />;
}